function config_entry(Entry) 
end
function after_create_entry(Entry) 
end
function after_destroy_entry_ctf(Entry)
end
function after_player_login_ctf(Entry, Player)
end
function check_can_enter_ctf(Player, CopyMGR)
end
function begin_enter_ctf(Player, CopyMGR)
end
