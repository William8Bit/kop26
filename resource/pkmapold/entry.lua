
function config_entry(Entry) 
end
function after_create_entry(Entry) 
end
function after_destroy_entry_pkmap(Entry)
end
function after_player_login_pkmap(Entry, Player)
end
function check_can_enter_pkmap(Player, CopyMGR)
end
function begin_enter_pkmap(Player, CopyMGR)
end
